<h2>Show</h2>

<p><%= link_to (@teacher.title + " " + @teacher.name), edit_school_teacher_path(@school.id, @teacher.id) %></p>
<p><%= @teacher.department.name %></p>
<p><%= @teacher.role.name %></p>

<%= link_to 'edit', edit_school_teacher_path(@school.id, @teacher.id) %> | <%= link_to 'back', school_teachers_path %>
