
<%= form_for @department do |f| %>
  <p><%= f.label :name %> <%= f.text_field :name %></p>
  <p><%= f.collection_select(:school_id, School.all, :id, :name, prompt: true ) %></p>
  <p><%= f.submit "submit" %>

<% end %>
